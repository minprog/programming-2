formulas:
    webserver: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
    mispell: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
    finance: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
    mashup: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
    python: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
    rss: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)

calculation:
    resit:
        resit: 0
        psets: 100
    normal:
        quiz: 0
        psets: 100

psets:
    type: integer
    drop: correctness
    grades:
        breakout: 1
        recover: 1
        mispell: 1
        py1: 1
        py2: 1
        py3: 1

quiz:
    type: pass
    required: true
    grades:
        quiz: 1

resit:
    type: pass
    required: true
    grades:
        resit: 1
